# 3D 视图鼠标操作快速参考

## ??? 鼠标操作（当前配置）

| 鼠标按键 | 操作 | 图示 |
|---------|------|------|
| **中键拖动** | 旋转视图 | ?? |
| **右键拖动** | 平移视图 | ???????? |
| **滚轮** | 缩放视图 | ?? |

---

## ? 快速记忆

```
??? 中键 = ?? 旋转 (主要操作)
??? 右键 = ???? 平移 (辅助操作)
??? 滚轮 = ?? 缩放
```

---

## ?? 配置说明

### 当前配置（HelixToolkit 默认）
- ? **中键拖动** → 旋转视图
- ? **右键拖动** → 平移视图
- ? **滚轮** → 缩放视图

这是 HelixToolkit.Wpf 的默认鼠标手势配置。

---

## ?? 常用操作组合

### 查看步骤高亮
```
1. 选择步骤
2. 中键拖动 → 旋转查看 ??
3. 滚轮 → 放大标签 ??
4. 右键拖动 → 调整位置 ????
```

### 检查曲线
```
1. 中键拖动 → 多角度查看 ??
2. 滚轮 → 放大细节 ??
3. 右键拖动 → 移动视图 ????
```

---

## ?? 优势

- ? HelixToolkit 标准配置
- ? 中键旋转符合工程习惯
- ? 右键平移作为辅助功能

---

**详细文档**: `3D视图鼠标按键调换说明.md`
