# UI 简化快速参考

## ? 修改内容

### 1. 删除"放样选中曲线"

| 位置 | 状态 |
|------|------|
| **菜单栏** | ? 已删除 |
| **工具栏** | ? 已删除 |

### 2. 简化放样点数设置

| 控件 | 状态 |
|------|------|
| **滑动条** | ? 已删除 |
| **输入框** | ? 保留（增强）|

---

## ?? UI 对比

### 修改前
```
放样菜单:
├─ 放样选中曲线(_S)  ← 删除
└─ 放样所有曲线(_A)

工具栏:
[?? 导入] [??? 命名] | [?? 放样] [?? 放样全部] [?? 反转]
             ↑ 删除

放样点数:
┌─────────────────┐
│ ═══════●═══  10  │  ← 滑动条+输入框
└─────────────────┘
```

### 修改后
```
放样菜单:
└─ 放样所有曲线(_A)  ← 只保留这个

工具栏:
[?? 导入] [??? 命名] | [?? 放样全部] [?? 反转]
    ↑ 删除了"放样"按钮

放样点数:
┌─────────────────┐
│  10      │  ← 只有输入框（居中）
└─────────────────┘
```

---

## ?? 保留的功能

| 功能 | 菜单 | 工具栏 | 快捷键 |
|------|------|--------|--------|
| **放样所有曲线** | ? | ? | Ctrl+Shift+L |
| **反转曲线** | ? | ? | Ctrl+R |
| **自动命名** | ? | ? | - |
| **导入STEP** | ? | ? | Ctrl+I |
| **生成步骤** | ? | ? | Ctrl+G |
| **导出XML** | ? | ? | Ctrl+S |

---

## ?? 新的操作流程

```
1. 导入 STEP 文件
   ↓
2. 输入放样点数（例如：10）
   ↓
3. 点击"放样全部"
   ↓
4. 所有曲线自动放样
```

---

## ?? 优势

- ? **界面更简洁** - 减少冗余控件
- ? **操作更直接** - 统一使用"放样全部"
- ? **输入更快速** - 直接输入数值

---

**详细文档**: `UI简化修改总结.md`
